
version: 0.0

os: windows
#
files:
  - source: /
    destination: /home/ec2-user/nodejsdiscordbot
hooks:
  BeforeInstall:
    - location: bin/install.bat
      timeout: 300

  ApplicationStop:
    - location: bin/app-stop.bat
      timeout: 30

  AfterInstall:
    - location: bin/install-pkgs.bat
      timeout: 1200

  ApplicationStart:
    - location: bin/app-start.bat
      timeout: 60

